{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug on Localhost (XAMPP)",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost/practice/home.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}